name: Secrets test

on:
  push:
    branches:
      - "master"
  pull_request:
    types:
      - "opened"
      - "reopened"
      - "synchronize"
  workflow_dispatch:

jobs:
  secrets-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo secret DB_PASSWORD is ${{secrets.DB_PASSWORD}}
